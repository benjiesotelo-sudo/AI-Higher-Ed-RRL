__pycache__/
*.pyc
.venv/
.env
.pytest_cache/
.ruff_cache/
.mypy_cache/
data/
pdfs/
logs/
output/run_manifest_*.json
output/~$*.xlsx
*.egg-info/
.coverage
.DS_Store
.Rhistory

# Private manuscript work-in-progress — fully excluded (never shared publicly)
Manuscript/

# Internal dev artifacts — kept locally, not published.
# This repo is public and meant to be cloned to reproduce harvesting/screening;
# these expose internal process/planning rather than help reproduce the pipeline.
PROGRESS.md
docs/
scripts/build_manuscript_docx.py

# Local-only dustbin for backup / removed / superseded files
OLD Files/

# Segregated high-confidence-with-PDF subset for the (future) MMAT appraisal.
# Copies of copyrighted full-text PDFs — never published.
MMAT Appraisal Set/

# MMAT engine work files (full copyrighted text + verbatim quotes — never commit)
*.work.jsonl
*.answers.jsonl
*.rejects.jsonl
MMAT_Dean_Review/
MMAT_*.zip
work_parts/
